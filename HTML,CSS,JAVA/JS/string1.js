// String Day 2
let name = 'John Cena';

// uppercase - 
console.log(name.toUpperCase());
// Lowercase - 
console.log(name.toLowerCase());
// includes() - check if string belong to string
console.log(name.includes('Wick'), name.includes('John'));
// indexing - access individual character in a string
// slicing - access multiple character in a string